pipeline {
    agent any
    stages {
        stage('checkout') {
            steps {
                sh 'echo checkout'
            }
        }
        stage('build') {
            steps {
                sh 'echo build'
            }
         }
         stage('test') {
            steps {
                sh 'echo test'

            }
         }
         stage('publish') {
            steps {
                sh 'echo publish'

            }
        }
    }
}
